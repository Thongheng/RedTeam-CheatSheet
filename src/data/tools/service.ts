import type { Tool } from '../../types';
import { ARG_CREDS, ARG_SHARE, ARG_SHARE_NAME, createArg } from './common';

export const SERVICE_TOOLS: Tool[] = [
    // --- SERVICE (Service Enumeration) -> SMB ---
];
